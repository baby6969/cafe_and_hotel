# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration - MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster0.fkkmrws.mongodb.net/culinary-canvas?retryWrites=true&w=majority&appName=Cluster0
# Local MongoDB (uncomment if using local MongoDB instead)
# MONGODB_URI=mongodb://localhost:27017/culinary-canvas
# Alternative for SQLite (uncomment if using SQLite instead)
# DB_PATH=./database.sqlite

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-2024
JWT_EXPIRES_IN=24h
RESET_TOKEN_EXPIRES_IN=1h

# Admin Default Credentials (for first-time setup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@culinarycanvas.com

# Email Configuration (for password recovery)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@goldenbean.com

# Frontend URL (for CORS and reset links)
FRONTEND_URL=http://localhost:3000

